Multiply function
This will multyply two numbers
the second function does a square root of the input and then multiplys the numbers. the return is the multiplication of the two.


We added the subtract function, it can subtract 2 numbers. 



ADD function:
The add function uses two numbers to calculate the folowing additions: a+b, 5a+b, a+7b. 

SUM function is added, now you can determine the sum of a list. 

Function "func": It return the distance from Zero. In other words, the absolute value or modulus of a number. F.e.: if you give the number -5, it will return 5, and you give the number 10, it will return 10.

